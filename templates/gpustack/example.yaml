- name: deployment_name
  value: "gpustack-test"
  default: "gpustack"
  editable: true
  required: true
  description: "deployment name"

- name: version
  value: "v2.0.2"
  default: "v2.0.2"
  editable: true
  required: false
  description: "GPUStack version to deploy. See releases: https://github.com/gpustack/gpustack/releases"

- name: admin_password
  value: "password"
  default: "password"
  editable: true
  required: false
  description: "Default password name for admin user"

- name: working_memory
  value: 100
  default: 5
  editable: true
  required: false
  description: "Temporary storage to use to cache model weights (in GB)"

- name: nvidia_gpus
  value: 2
  default: 1
  editable: true
  required: true
  description: "Number of remote workers (for tensor and pipeline parallelism). This is in addition to the main node"

- name: amd_gpus
  value: 0
  default: 1
  editable: true
  required: true
  description: "Number of remote workers (for tensor and pipeline parallelism). This is in addition to the main node"

- name: token
  value: "randomkey"
  default: "sometoken"
  editable: true
  required: false
  description: " to load"

- name: hf_token
  value: token
  default: null
  editable: true
  required: false
  description: "Huggingface token, required to load model weights"

- name: cpus
  value: 2
  default: 2
  editable: true
  required: false
  description: "CPUs per single worker (final one = cpus * num_workers)"

- name: memory
  value: 12
  default: 12
  editable: true
  required: false
  description: "RAM memory per single worker (final one = memory * num_workers)"

- name: cuda_gpu_mem_percentage
  value: 100
  default: 100
  editable: true
  required: false
  description: "Maximum memory fraction allowed to be used from the GPU vRAM."

- name: nodeport
  value: "30963"
  default: ""
  editable: true
  required: false
  description: "Specify the node port for the service that exposes the job"