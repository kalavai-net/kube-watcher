############################
## MUST HAVE THESE FIELDS ##
- name: id_field
  value: deployment_name
  default: deployment_name
  editable: false
  required: false
  description: "Field that contains the ID of the job"
############################

- name: deployment_name
  value: "https-portal" 
  default: "https-portal"
  editable: true
  required: true
  description: "Name of the job"

- name: domains
  value: "api.cogenai.kalavai.net -> http://51.159.150.251:30861"
  default: "api.cogenai.kalavai.net -> http://51.159.150.251:30861"
  editable: true
  required: true
  description: "Domains to map to the job"

- name: stage
  value: "production"
  default: "production"
  editable: true
  required: false
  description: "Environment stage"

- name: force_renew
  value: "true"
  default: "true"
  editable: true
  required: false
  description: "Force renew of certificates"

- name: max_retries
  value: 1000
  default: 1000
  editable: true
  required: false
  description: "If the job fails, how many times to retry redeployment"

- name: http_nodeport
  value: ""
  default: ""
  editable: true
  required: false
  description: "Specify the node port for the service that exposes the job (http)"

- name: https_nodeport
  value: ""
  default: ""
  editable: true
  required: false
  description: "Specify the node port for the service that exposes the job (https)"